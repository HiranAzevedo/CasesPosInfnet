@model IEnumerable<ProjetoModeloDDD.MVC.ViewModels.PublicacaoComentarioViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdPerfil)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PublicacaoText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdPerfil)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PublicacaoText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdPublicacao }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdPublicacao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdPublicacao })
        </td>
    </tr>
}

</table>
